###############################################################################
# Supabase CLI / config.toml 환경변수 치환용 (.env)
#
# supabase/config.toml 에서 env(S3_HOST) 같은 값을 사용할 때,
# 아래 파일을 `supabase/.env` 로 복사해 두면 로컬에서 쉽게 관리할 수 있습니다.
#
# 사용법:
#   cp supabase/.env.example supabase/.env
###############################################################################

S3_HOST=
S3_REGION=
S3_ACCESS_KEY=
S3_SECRET_KEY=

